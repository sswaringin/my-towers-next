import type { NextConfig } from "next";
// import path from "path";

const nextConfig: NextConfig = {
  // output: "export",
  // turbopack: {
  //   root: path.join(__dirname, ".."), // needed for using locally developed modules
  // },
};

export default nextConfig;
